# CANOPY Toy 生产发布记录｜2026-07-16

## 结论

Toy ID `10228414336000` 已于 2026-07-16 20:30:29（UTC+8）变为 `published`。正式地址：[绿色地狱网页版](https://www.bilibili.com/toy/green-hell-web/index.html)。

提交时 CLI 返回 `auditing`，审核预览为 [preview_M85HKubX](https://www.bilibili.com/toy/preview/preview_M85HKubX/index.html)；`auditing` 与最终 `published` 是两个独立状态。

## 制品

| 字段 | 值 |
| --- | --- |
| 构建目录 | `toy-out/` |
| 文件数 | 20 |
| 总字节数 | 5,047,364 |
| 规范化清单 SHA-256 | `0e16435b7931f005af251138880516f29f1fee3541f281d88cbe9521d87b0034` |
| 每文件清单 | [2026-07-16-toy-manifest.tsv](2026-07-16-toy-manifest.tsv) |
| Node | v22.18.0 |
| Toy CLI | v0.3.0 |
| 发布账号 | UID `4153976` / `上只当麻_1047008888` |
| Toy doctor | `ok: true`，0 findings |

规范化摘要算法：按相对路径排序，每行使用 `relativePath<TAB>bytes<TAB>fileSha256`，UTF-8 编码，以 LF 连接且末尾不追加 LF，再计算整体 SHA-256。

## 本地门禁

- 636 / 636 自动化测试通过。
- TypeScript、ESLint、Vinext 生产构建和 Toy 专用构建通过。
- Toy doctor 无 findings。
- 预览阶段完成桌面、390×844 竖屏和 844×390 短横屏聚焦检查。

## 生产冒烟

2026-07-16 21:08–21:24（UTC+8）在正式地址完成：

- 首屏正确显示 CANOPY 标题、主线描述和开始/继续入口；
- “开始新远征”在已有进度时正确显示覆盖确认，没有执行删除；
- “继续最近存档”成功恢复 Day 03 进度；
- WebGL 游戏场景、任务、状态、装备 1–5 和世界 HUD 正常出现；
- 未发现本游戏 CSS/JS 404 或启动错误。

控制台仍有 B 站宿主脚本 `bili-sc-sdk.umd.js` 的 `Identifier 'g' has already been declared`。该错误不来自本游戏制品，本次场景仍能正常运行；后续若宿主能力异常，应继续向平台侧追踪。

## 完成通知

2026-07-16 21:47（UTC+8）已向飞书会话“王鲸Codex”发送正式发布完成通知，并在会话中确认消息出现。通知包含 `published` 状态、正式地址、Toy ID、本次验收结果、经验手册，以及未与唯一 Git SHA 绑定和仍未完成能力的真实边界。

## 诚实边界与已知风险

- 本制品从有未提交改动的工作树构建，构建时 HEAD 基线为 `6ad886f12bca0cdeacc3e4138cfc9ad3778cab3b`，但**不能声称制品等于该 Git commit**。
- 当前 manifest 可验证发布制品本身，不能替代来源代码快照；下一版必须从干净、唯一 commit 构建并绑定 manifest。
- 未完成广泛真实手机、Pointer Lock 手感、Toy 云跨设备恢复、冲突合并和长时生产稳定性验证。
- `wiki-canopy-survival.png` 位于主游戏包中但未检索到运行时引用，约 1.04 MiB；下一版应确认资产归属并移出无关制品或补齐许可来源。
- 完整三小时内容、完整导演、完整生态链和最终 Valheim 式画面升级仍未完成。
